<section class="channels">
    <h1 class="h2"><%= t '.title', provider: @channel.pretty_provider %></h1>
    <article class="box">
        <div class="box-bd">
            <%= form_for @channel, as: :channel, url: {action: :create} do |f| %>
            <%= render 'form', f: f %>
            <% end %>
        </div>
    </article>
</section>