<h1 class="h2 cTxt"><%= t('.title') %></h1>
<div class="narrow">
    <div class="box">
        <div class="box-bd">
            <div class="row">
                <div class="cell ratio1">
                    <%= form_tag channel_unsubscription_path(channel_id: @channel.id) do %>
                        <%= email_field_tag :email, nil, placeholder: t('.email_placeholder') %>
                        <%= button_tag t('.submit'), class: 'butn action' %>
                    <% end %>
                </div>
            </div>
        </div>
    </div>
</div>
